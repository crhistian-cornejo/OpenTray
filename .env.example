# ============================================
# OpenTray - macOS Code Signing & Notarization
# ============================================
# Copy this file to .env and fill in your values
# 
# Find your signing identity with:
#   security find-identity -v -p codesigning
#
# The build script auto-detects the signing identity,
# but you can override it here:
# APPLE_SIGNING_IDENTITY="Developer ID Application: Your Name (TEAM_ID)"

# ============================================
# Notarization (Required for public distribution)
# ============================================
# 
# Option 1: Keychain Profile (RECOMMENDED)
# -----------------------------------------
# Create once with:
#   xcrun notarytool store-credentials "OpenTray" \
#     --apple-id "your-email@example.com" \
#     --team-id "YOUR_TEAM_ID" \
#     --password "app-specific-password"
#
# Then set:
APPLE_KEYCHAIN_PROFILE=OpenTray

# Option 2: Direct Credentials
# ----------------------------
# Less secure - credentials in plain text
# Create app-specific password at: https://appleid.apple.com
#
# APPLE_ID=your-apple-id@email.com
# APPLE_PASSWORD=xxxx-xxxx-xxxx-xxxx
# APPLE_TEAM_ID=XXXXXXXXXX

# ============================================
# Build Options
# ============================================
# Skip notarization (for testing)
# SKIP_NOTARIZE=1

# Build for specific target
# TARGET=aarch64-apple-darwin
# TARGET=x86_64-apple-darwin
